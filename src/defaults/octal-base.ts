import CustomBase from '../custom-base';

/**
 * Instance of the octal base, using the symbols 0 through 7.
 * The octal system is a base-8 number system, commonly used in computing.
 *
 * @type {CustomBase}
 * @constant
 */
const octalBase = new CustomBase([
  '0', '1', '2', '3', '4', '5', '6', '7'
]);
export default octalBase;
